# i18n Multi-Language UI Support Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add English, French, Spanish, German, Portuguese, and Arabic UI translations to the Fennex dashboard using react-i18next with cookie-based locale persistence and cross-device sync via the user profile.

**Architecture:** All UI strings move to `public/locales/{lang}/common.json` files served statically. A `fennex_lang` cookie provides instant locale init on page load (no flash). `I18nProvider` syncs `user.language` from `GET /users/me` so a user's preference follows them across devices. Language changes are persisted via `PATCH /users/me/language`.

**Tech Stack:** `react-i18next`, `i18next`, `i18next-browser-languagedetector`, `i18next-http-backend`, Alembic for migration, FastAPI/SQLAlchemy for the backend endpoint.

## Global Constraints

- Supported locales: `en`, `fr`, `es`, `de`, `pt`, `ar` — exactly these six, no others
- Cookie name: `fennex_lang`; max-age 1 year; `path=/; SameSite=Lax`
- Single namespace: `common` — all keys go in `public/locales/{lang}/common.json`
- Backend validates `language` against `SUPPORTED_LOCALES = {"en", "fr", "es", "de", "pt", "ar"}`; returns 400 for anything else
- Migration revision ID: `l7g8b9c0d1e2`, revises `k6f7a8b9c0d1`
- Migration file: `apps/api/alembic/versions/l7g8b9c0d1e2_user_language.py`
- No URL-based locale (`/fr/dashboard` etc.) — cookie only
- No RTL CSS for Arabic in this phase (strings translated; layout is follow-up)
- `useTranslation()` called once per component at the top level; no `Trans` component needed; interpolation via `t("key", { var: value })`

---

## File Map

**Created:**
- `apps/api/alembic/versions/l7g8b9c0d1e2_user_language.py` — adds `language` column to `users`
- `apps/web/lib/i18n.ts` — i18next init with cookie detector
- `apps/web/components/providers/I18nProvider.tsx` — wraps root layout, syncs user.language
- `apps/web/components/layout/LanguagePicker.tsx` — flag+name dropdown, used in TopBar and Settings
- `public/locales/en/common.json` — complete English strings (source of truth)
- `public/locales/fr/common.json` — French (stub keys matching en)
- `public/locales/es/common.json` — Spanish (stub)
- `public/locales/de/common.json` — German (stub)
- `public/locales/pt/common.json` — Portuguese (stub)
- `public/locales/ar/common.json` — Arabic (stub)

**Modified:**
- `apps/api/app/models/user.py` — add `language` column
- `apps/api/app/api/v1/routers/users.py` — add `language` to `UserProfile`, implement `PATCH /users/me/language`
- `apps/web/lib/api.ts` — add `language` to `UserProfile`, add `updateMyLanguage()`
- `apps/web/app/layout.tsx` — wrap with `I18nProvider`
- `apps/web/components/layout/TopBar.tsx` — add `LanguagePicker`, apply `t()`
- `apps/web/components/layout/Sidebar.tsx` — apply `t()` to nav labels
- `apps/web/components/layout/CommandPalette.tsx` — apply `t()` to command labels
- `apps/web/app/(auth)/login/page.tsx` — apply `t()`
- `apps/web/app/(auth)/register/page.tsx` — apply `t()`
- `apps/web/app/(dashboard)/page.tsx` — apply `t()`
- `apps/web/app/(dashboard)/brand-voice/page.tsx` — apply `t()`
- `apps/web/app/(dashboard)/settings/page.tsx` — add `LanguagePicker`, apply `t()`
- `apps/web/components/billing/UpgradeModal.tsx` — apply `t()`
- `apps/web/components/billing/UsageBanner.tsx` — apply `t()`
- `apps/web/app/(dashboard)/[projectId]/overview/page.tsx` — apply `t()`
- `apps/web/app/(dashboard)/[projectId]/articles/page.tsx` — apply `t()`
- `apps/web/app/(dashboard)/[projectId]/keywords/page.tsx` — apply `t()`
- `apps/web/app/(dashboard)/[projectId]/analytics/page.tsx` — apply `t()`
- `apps/web/app/(dashboard)/[projectId]/images/page.tsx` — apply `t()`
- `apps/web/app/(dashboard)/[projectId]/social/page.tsx` — apply `t()`
- `apps/web/app/(dashboard)/[projectId]/content/page.tsx` — apply `t()`
- `apps/web/app/(dashboard)/[projectId]/audit/page.tsx` — apply `t()`
- `apps/web/app/(dashboard)/[projectId]/backlinks/page.tsx` — apply `t()`
- `apps/web/app/(dashboard)/[projectId]/publishing/page.tsx` — apply `t()`
- `apps/web/app/(dashboard)/[projectId]/loading.tsx` (if exists) — not needed; loading skeletons have no strings

---

### Task 1: Backend — User language column, migration, updated UserProfile, PATCH endpoint

**Files:**
- Create: `apps/api/alembic/versions/l7g8b9c0d1e2_user_language.py`
- Modify: `apps/api/app/models/user.py`
- Modify: `apps/api/app/api/v1/routers/users.py`
- Test: `apps/api/tests/test_users_language.py` (new)

**Interfaces:**
- Produces: `GET /users/me` → `{ ..., language: "en" }` (UserProfile gains `language: str`)
- Produces: `PATCH /api/v1/users/me/language` body `{ "language": "fr" }` → `{ "language": "fr" }` or 400

- [ ] **Step 1: Write the failing tests**

Create `apps/api/tests/test_users_language.py`:

```python
"""Tests for user language preference endpoint."""
import uuid
import pytest
from httpx import AsyncClient, ASGITransport
from sqlalchemy.ext.asyncio import AsyncSession, async_sessionmaker, create_async_engine

from app.core.database import Base
from app.core.dependencies import get_current_user, get_db
from app.main import app as fastapi_app
from app.models.organization import Organization, PlanTier
from app.models.user import User, UserRole

TEST_DATABASE_URL = "sqlite+aiosqlite:///:memory:"
test_engine = create_async_engine(TEST_DATABASE_URL, echo=False)
TestSessionLocal = async_sessionmaker(test_engine, expire_on_commit=False, class_=AsyncSession)

FAKE_ORG_ID = uuid.uuid4()
FAKE_USER_ID = uuid.uuid4()


async def override_get_db():
    async with TestSessionLocal() as session:
        try:
            yield session
            await session.commit()
        except Exception:
            await session.rollback()
            raise


async def override_get_current_user():
    return User(
        id=FAKE_USER_ID, org_id=FAKE_ORG_ID, email="test@test.com",
        hashed_password="x", full_name="Test User", role=UserRole.OWNER,
        is_active=True, language="en",
    )


@pytest.fixture(autouse=True)
async def setup_db():
    tables = [Base.metadata.tables[n] for n in ("organizations", "users") if n in Base.metadata.tables]
    async with test_engine.begin() as conn:
        await conn.run_sync(Base.metadata.create_all, tables=tables)
    async with TestSessionLocal() as session:
        session.add(Organization(id=FAKE_ORG_ID, slug="test", name="Test Org", plan_tier=PlanTier.FREE))
        session.add(User(
            id=FAKE_USER_ID, org_id=FAKE_ORG_ID, email="test@test.com",
            hashed_password="x", full_name="Test User", role=UserRole.OWNER,
            is_active=True, language="en",
        ))
        await session.commit()
    fastapi_app.dependency_overrides[get_db] = override_get_db
    fastapi_app.dependency_overrides[get_current_user] = override_get_current_user
    yield
    fastapi_app.dependency_overrides.clear()
    async with test_engine.begin() as conn:
        await conn.run_sync(Base.metadata.drop_all, tables=tables)


@pytest.fixture
async def client():
    async with AsyncClient(transport=ASGITransport(app=fastapi_app), base_url="http://test") as ac:
        yield ac


async def test_get_me_includes_language(client):
    """GET /users/me returns language field."""
    resp = await client.get("/api/v1/users/me", headers={"Authorization": "Bearer fake"})
    assert resp.status_code == 200
    assert resp.json()["language"] == "en"


async def test_patch_language_valid(client):
    """PATCH /users/me/language with supported locale returns updated language."""
    resp = await client.patch(
        "/api/v1/users/me/language",
        json={"language": "fr"},
        headers={"Authorization": "Bearer fake"},
    )
    assert resp.status_code == 200
    assert resp.json()["language"] == "fr"


async def test_patch_language_invalid(client):
    """PATCH /users/me/language with unsupported locale returns 400."""
    resp = await client.patch(
        "/api/v1/users/me/language",
        json={"language": "zh"},
        headers={"Authorization": "Bearer fake"},
    )
    assert resp.status_code == 400
    assert "not supported" in resp.json()["detail"].lower()
```

- [ ] **Step 2: Run tests to verify they fail**

```bash
cd apps/api && python -m pytest tests/test_users_language.py -v
```

Expected: FAIL — `language` field missing from User model and UserProfile, endpoint doesn't exist.

- [ ] **Step 3: Add `language` column to User model**

In `apps/api/app/models/user.py`, add after `is_active`:

```python
language: Mapped[str] = mapped_column(String(5), nullable=False, server_default="en")
```

Full relevant section of `user.py` after edit:

```python
from sqlalchemy.orm import Mapped, mapped_column
from sqlalchemy import String, Boolean, Enum as SAEnum
# ... existing imports ...

class User(Base):
    __tablename__ = "users"

    id: Mapped[uuid.UUID] = mapped_column(primary_key=True, default=uuid.uuid4)
    org_id: Mapped[uuid.UUID] = mapped_column(ForeignKey("organizations.id"), nullable=False)
    email: Mapped[str] = mapped_column(String(255), unique=True, nullable=False, index=True)
    hashed_password: Mapped[str] = mapped_column(String(255), nullable=False)
    full_name: Mapped[Optional[str]] = mapped_column(String(255), nullable=True)
    avatar_url: Mapped[Optional[str]] = mapped_column(String(512), nullable=True)
    role: Mapped[UserRole] = mapped_column(SAEnum(UserRole), nullable=False, default=UserRole.MEMBER)
    is_active: Mapped[bool] = mapped_column(Boolean, nullable=False, default=True)
    locked: Mapped[bool] = mapped_column(Boolean, nullable=False, default=False)
    locked_reason: Mapped[Optional[str]] = mapped_column(String(255), nullable=True)
    language: Mapped[str] = mapped_column(String(5), nullable=False, server_default="en")
```

- [ ] **Step 4: Create Alembic migration**

Create `apps/api/alembic/versions/l7g8b9c0d1e2_user_language.py`:

```python
"""Add language column to users

Revision ID: l7g8b9c0d1e2
Revises: k6f7a8b9c0d1
Create Date: 2026-06-29
"""
from alembic import op
import sqlalchemy as sa

revision = "l7g8b9c0d1e2"
down_revision = "k6f7a8b9c0d1"
branch_labels = None
depends_on = None


def upgrade() -> None:
    op.add_column(
        "users",
        sa.Column("language", sa.String(5), nullable=False, server_default="en"),
    )


def downgrade() -> None:
    op.drop_column("users", "language")
```

- [ ] **Step 5: Update `UserProfile` and add endpoint in `users.py`**

In `apps/api/app/api/v1/routers/users.py`, make these changes:

```python
from fastapi import APIRouter, HTTPException
from pydantic import BaseModel
from app.core.dependencies import CurrentUser, DB

router = APIRouter()

SUPPORTED_LOCALES = {"en", "fr", "es", "de", "pt", "ar"}


class UserProfile(BaseModel):
    id: str
    email: str
    full_name: str | None
    avatar_url: str | None
    role: str
    org_id: str
    language: str


class UpdateLanguageRequest(BaseModel):
    language: str


@router.get("/me", response_model=UserProfile)
async def get_me(current_user: CurrentUser):
    return UserProfile(
        id=str(current_user.id),
        email=current_user.email,
        full_name=current_user.full_name,
        avatar_url=current_user.avatar_url,
        role=current_user.role.value if hasattr(current_user.role, "value") else current_user.role,
        org_id=str(current_user.org_id),
        language=current_user.language,
    )


@router.patch("/me")
async def update_me(current_user: CurrentUser):
    return {"message": "Not implemented yet"}


@router.patch("/me/language")
async def update_language(body: UpdateLanguageRequest, current_user: CurrentUser, db: DB):
    if body.language not in SUPPORTED_LOCALES:
        raise HTTPException(
            status_code=400,
            detail=f"Language '{body.language}' is not supported. Supported: {sorted(SUPPORTED_LOCALES)}",
        )
    current_user.language = body.language
    await db.commit()
    return {"language": body.language}
```

- [ ] **Step 6: Run tests to verify they pass**

```bash
cd apps/api && python -m pytest tests/test_users_language.py -v
```

Expected: PASS — all 3 tests green.

- [ ] **Step 7: Run full test suite to check for regressions**

```bash
cd apps/api && python -m pytest --tb=short -q
```

Expected: all previously-passing tests still pass.

- [ ] **Step 8: Commit**

```bash
git add apps/api/app/models/user.py \
        apps/api/app/api/v1/routers/users.py \
        apps/api/alembic/versions/l7g8b9c0d1e2_user_language.py \
        apps/api/tests/test_users_language.py
git commit -m "feat(api): add language preference column + PATCH /users/me/language endpoint"
```

---

### Task 2: i18n Foundation — packages, `lib/i18n.ts`, `I18nProvider`, layout wiring, api.ts additions

**Files:**
- Modify: `apps/web/package.json` (add dependencies)
- Create: `apps/web/lib/i18n.ts`
- Create: `apps/web/components/providers/I18nProvider.tsx`
- Modify: `apps/web/app/layout.tsx`
- Modify: `apps/web/lib/api.ts`

**Interfaces:**
- Produces: `i18n` default export from `@/lib/i18n` — configured i18next instance
- Produces: `I18nProvider` component — wraps children, syncs user language
- Produces: `SUPPORTED_LOCALES` const and `Locale` type from `@/lib/i18n`
- Produces: `updateMyLanguage(language: string): Promise<{ language: string }>` from `@/lib/api`
- Produces: `UserProfile.language: string` — already returned by backend from Task 1

- [ ] **Step 1: Install packages**

```bash
cd apps/web && pnpm add i18next react-i18next i18next-browser-languagedetector i18next-http-backend
```

Verify `apps/web/package.json` now lists all four packages in `dependencies`.

- [ ] **Step 2: Create `apps/web/lib/i18n.ts`**

```typescript
import i18n from "i18next";
import { initReactI18next } from "react-i18next";
import LanguageDetector from "i18next-browser-languagedetector";
import Backend from "i18next-http-backend";

export const SUPPORTED_LOCALES = ["en", "fr", "es", "de", "pt", "ar"] as const;
export type Locale = (typeof SUPPORTED_LOCALES)[number];

i18n
  .use(Backend)
  .use(LanguageDetector)
  .use(initReactI18next)
  .init({
    fallbackLng: "en",
    supportedLngs: SUPPORTED_LOCALES,
    ns: ["common"],
    defaultNS: "common",
    backend: { loadPath: "/locales/{{lng}}/{{ns}}.json" },
    detection: {
      order: ["cookie", "navigator"],
      lookupCookie: "fennex_lang",
      caches: ["cookie"],
      cookieOptions: { maxAge: 365 * 24 * 3600, path: "/", sameSite: "Lax" },
    },
    interpolation: { escapeValue: false },
  });

export default i18n;
```

- [ ] **Step 3: Create `apps/web/components/providers/` directory and `I18nProvider.tsx`**

```bash
mkdir -p apps/web/components/providers
```

Create `apps/web/components/providers/I18nProvider.tsx`:

```typescript
"use client";
import { useEffect } from "react";
import { I18nextProvider } from "react-i18next";
import { useQuery } from "@tanstack/react-query";
import i18n from "@/lib/i18n";
import { getMe, isAuthenticated } from "@/lib/api";

export function I18nProvider({ children }: { children: React.ReactNode }) {
  const { data: me } = useQuery({
    queryKey: ["me"],
    queryFn: getMe,
    enabled: isAuthenticated(),
    staleTime: 5 * 60_000,
  });

  useEffect(() => {
    if (me?.language && me.language !== i18n.language) {
      i18n.changeLanguage(me.language);
    }
  }, [me?.language]);

  return <I18nextProvider i18n={i18n}>{children}</I18nextProvider>;
}
```

- [ ] **Step 4: Wire `I18nProvider` into root layout**

In `apps/web/app/layout.tsx`, import and wrap the children with `I18nProvider`. Find the existing return JSX and wrap the innermost children (inside any existing QueryClientProvider / ThemeProvider etc.):

```typescript
import { I18nProvider } from "@/components/providers/I18nProvider";

// Inside the JSX tree, wrap children:
<I18nProvider>
  {children}
</I18nProvider>
```

The exact wrapping position: `I18nProvider` must be **inside** any existing `QueryClientProvider` (because it calls `useQuery`) and **outside** all page content. If the layout already has a `Providers` component, add `I18nProvider` inside it after `QueryClientProvider`.

- [ ] **Step 5: Add `language` to `UserProfile` and `updateMyLanguage` to `apps/web/lib/api.ts`**

Find the `UserProfile` interface and add `language: string`:

```typescript
export interface UserProfile {
  id: string;
  email: string;
  full_name: string | null;
  avatar_url: string | null;
  role: string;
  org_id: string;
  language: string;  // ← add this
}
```

Add the `updateMyLanguage` function near the other user-related functions:

```typescript
export async function updateMyLanguage(language: string): Promise<{ language: string }> {
  return apiClient.patch<{ language: string }>("/users/me/language", { language });
}
```

- [ ] **Step 6: Verify TypeScript compiles**

```bash
cd apps/web && pnpm tsc --noEmit 2>&1 | head -40
```

Expected: no errors related to the new files.

- [ ] **Step 7: Commit**

```bash
git add apps/web/package.json \
        apps/web/pnpm-lock.yaml \
        apps/web/lib/i18n.ts \
        apps/web/components/providers/I18nProvider.tsx \
        apps/web/app/layout.tsx \
        apps/web/lib/api.ts
git commit -m "feat(web): i18n foundation — i18next init, I18nProvider, api.ts language support"
```

---

### Task 3: English translation file — `public/locales/en/common.json`

**Files:**
- Create: `apps/web/public/locales/en/common.json`

**Interfaces:**
- Produces: complete key tree that all later `t()` calls reference

- [ ] **Step 1: Create the directory**

```bash
mkdir -p apps/web/public/locales/en
```

- [ ] **Step 2: Create `apps/web/public/locales/en/common.json`**

```json
{
  "nav": {
    "overview": "Overview",
    "keywords": "Keywords",
    "planner": "Planner",
    "articles": "Articles",
    "social": "Social",
    "images": "Images",
    "publishing": "Publishing",
    "backlinks": "Backlinks",
    "analytics": "Analytics",
    "audit": "Audit",
    "brandVoice": "Brand Voice",
    "settings": "Settings",
    "signOut": "Sign out",
    "createProject": "Create project",
    "newProject": "New project",
    "selectProject": "Select project",
    "research": "Research",
    "create": "Create",
    "grow": "Grow"
  },
  "common": {
    "save": "Save",
    "saving": "Saving…",
    "saved": "Saved",
    "cancel": "Cancel",
    "delete": "Delete",
    "deleting": "Deleting...",
    "edit": "Edit",
    "loading": "Loading…",
    "error": "Something went wrong",
    "upgrade": "Upgrade →",
    "currentPlan": "Current plan",
    "back": "Back",
    "done": "Done",
    "add": "Add",
    "create": "Create",
    "generating": "Generating...",
    "publish": "Publish",
    "publishing": "Publishing…",
    "connect": "Connect",
    "disconnect": "Disconnect",
    "copy": "Copy",
    "copied": "Copied",
    "test": "Test",
    "send": "Send",
    "accept": "Accept",
    "reject": "Reject",
    "verify": "Verify",
    "deactivate": "Deactivate",
    "activate": "Activate",
    "none": "None",
    "optional": "optional",
    "viewAll": "View all →",
    "previousPage": "← Previous",
    "nextPage": "Next →",
    "page": "Page {{n}}"
  },
  "auth": {
    "welcomeBack": "Welcome back",
    "signInSubtitle": "Sign in to your Fennex workspace",
    "email": "Email",
    "emailPlaceholder": "you@company.com",
    "password": "Password",
    "signIn": "Sign in →",
    "signingIn": "Signing in…",
    "newToFennex": "New to Fennex?",
    "createFreeAccount": "Create a free account →",
    "createAccount": "Create your account",
    "registerSubtitle": "Free to start · No credit card required",
    "fullName": "Full name",
    "company": "Company",
    "workEmail": "Work email",
    "passwordHint": "Min. 6 characters",
    "getStarted": "Get started for free →",
    "creatingAccount": "Creating account…",
    "haveAccount": "Have an account?",
    "signInInstead": "Sign in instead →",
    "termsText": "By continuing, you agree to our Terms of Service and Privacy Policy."
  },
  "topbar": {
    "searchPlaceholder": "Search or jump to…",
    "notifications": "Notifications",
    "allCaughtUp": "All caught up",
    "allCaughtUpDesc": "You're all caught up",
    "notificationsHint": "Job completions and alerts will show up here.",
    "loading": "Loading…"
  },
  "commandPalette": {
    "placeholder": "Search commands, pages, projects…",
    "noResults": "No results for \"{{query}}\"",
    "navigateHint": "navigate",
    "selectHint": "select",
    "toggleHint": "to toggle",
    "groups": {
      "navigate": "Navigate",
      "switchProject": "Switch project",
      "actions": "Actions"
    },
    "actions": {
      "lightMode": "Switch to light mode",
      "darkMode": "Switch to dark mode",
      "signOut": "Sign out"
    }
  },
  "dashboard": {
    "commandCenter": "Command Center",
    "welcomeBack": "Welcome back",
    "projectOverview": "Project overview",
    "noProjects": "No projects yet",
    "welcomeToFennex": "Welcome to Fennex",
    "tagline": "Your AI SEO growth platform.",
    "organicClicks": "Organic clicks · 28d",
    "connectSearchConsole": "Connect Search Console to see traffic trends.",
    "setup": "Setup",
    "complete": "complete",
    "quickActions": "Quick actions",
    "keywordResearch": "Keyword research",
    "generateArticle": "Generate article",
    "auditSite": "Audit your site",
    "recentArticles": "Recent articles",
    "noArticlesYet": "No articles yet",
    "generateFirstArticle": "Generate your first article →",
    "selectProject": "Select a project.",
    "setupSteps": {
      "createProject": "Create a project",
      "connectKeys": "Connect AI keys",
      "connectSearchConsole": "Connect Search Console",
      "generateArticle": "Generate an article"
    },
    "stats": {
      "impressions": "Impressions",
      "avgCtr": "Avg CTR",
      "avgPosition": "Avg Position",
      "published": "Published"
    }
  },
  "overview": {
    "title": "Project Overview",
    "subtitle": "Your organic growth at a glance.",
    "newArticle": "New article",
    "last28Days": "Last 28 days",
    "noAnalytics": "No analytics data yet.",
    "recentArticles": "Recent Articles",
    "viewAll": "View all →",
    "noArticles": "No articles yet.",
    "createOne": "Create one",
    "quickActions": "Quick Actions",
    "runKeywordResearch": "Run keyword research",
    "createArticle": "Create article",
    "viewAnalytics": "View analytics",
    "stats": {
      "clicks": "Clicks",
      "impressions": "Impressions",
      "avgCtr": "Avg CTR",
      "avgPosition": "Avg Position"
    }
  },
  "articles": {
    "title": "Articles",
    "subtitle": "AI-generated, SEO-optimized content.",
    "newArticle": "New Article",
    "allArticles": "All articles",
    "noArticles": "No articles yet",
    "selectArticle": "Select an article to edit",
    "selectArticleHint": "Pick one from the list, or generate a new AI-written draft.",
    "editor": {
      "edit": "Edit",
      "preview": "Preview",
      "save": "Save",
      "saving": "Saving…",
      "saved": "Saved",
      "publish": "Publish",
      "regenerate": "Regenerate",
      "regenerating": "Regenerating…",
      "saveRevision": "Save Revision",
      "breakdown": "Breakdown",
      "seoScore": "SEO Score",
      "metaTitle": "Meta title",
      "metaDescription": "Meta description",
      "articleTitle": "Article title",
      "words": "words"
    },
    "newArticleModal": {
      "title": "New Article",
      "hint": "AI will generate a full draft after creation.",
      "generating": "Generating your article…",
      "generatingHint": "This may take up to a minute. Hang tight.",
      "titleLabel": "Title",
      "keyword": "Target keyword",
      "tone": "Tone",
      "wordCount": "Word count target",
      "cancel": "Cancel",
      "create": "Create & Generate"
    },
    "publishModal": {
      "title": "Publish Article",
      "chooseConnection": "Choose connection",
      "loadingConnections": "Loading connections…",
      "noConnections": "No connections available.",
      "publishAs": "Publish as",
      "publishNow": "Publish Now",
      "publishedSuccess": "Published successfully",
      "viewPost": "View published post",
      "done": "Done"
    },
    "card": {
      "edit": "Edit",
      "regenerate": "Regenerate",
      "delete": "Delete"
    }
  },
  "keywords": {
    "title": "Keywords",
    "subtitle": "Discover high-ROI keyword opportunities and group them into clusters.",
    "enterKeyword": "Enter a keyword to start",
    "enterKeywordHint": "We'll research related keywords and group them into clusters.",
    "starting": "Starting…",
    "analyzing": "Analyzing…",
    "analyze": "Analyze",
    "discovering": "Discovering keywords and building clusters",
    "tableHeaders": {
      "keyword": "Keyword",
      "volume": "Volume",
      "difficulty": "Difficulty",
      "cpc": "CPC",
      "intent": "Intent"
    },
    "allKeywords": "All keywords",
    "clusters": "Clusters",
    "clearFilter": "Clear filter",
    "researchFailed": "Research failed",
    "unexpectedError": "An unexpected error occurred."
  },
  "analytics": {
    "title": "Analytics",
    "subtitle": "Search performance, rankings, and content impact.",
    "tabs": {
      "overview": "Overview",
      "rankings": "Rankings",
      "pagesQueries": "Pages & Queries",
      "content": "Content"
    },
    "dateRanges": {
      "7d": "7d",
      "28d": "28d",
      "90d": "90d"
    },
    "views": "Views",
    "dateRange": "Date range",
    "stats": {
      "clicks": "Clicks",
      "impressions": "Impressions",
      "avgCtr": "Avg CTR",
      "avgPosition": "Avg Position",
      "clicksImpressions": "Clicks & Impressions"
    },
    "searchConsoleConnected": "Search Console connected",
    "connectSearchConsole": "Connect Google Search Console to sync real traffic data.",
    "connect": "Connect",
    "disconnect": "Disconnect",
    "lastSynced": "Last synced",
    "noData": "No analytics data yet.",
    "sortBy": "Sort by:",
    "loadingRankings": "Loading rankings…",
    "noRankings": "No keyword rankings yet. Run keyword research first.",
    "topPages": "Top Pages",
    "topQueries": "Top Queries",
    "noPageData": "No page data yet.",
    "noQueryData": "No query data yet.",
    "publishArticles": "Publish articles to see their performance here.",
    "tableHeaders": {
      "keyword": "Keyword",
      "volume": "Volume",
      "intent": "Intent",
      "difficulty": "Difficulty",
      "position": "Position",
      "change": "Change",
      "clicks": "Clicks",
      "impressions": "Impressions",
      "ctr": "CTR",
      "avgPos": "Avg Pos",
      "article": "Article",
      "page": "Page",
      "query": "Query"
    }
  },
  "images": {
    "title": "Image Studio",
    "subtitle": "AI-generated visuals for articles and social posts.",
    "generate": "Generate Image",
    "noImages": "No images generated yet",
    "noImagesHint": "Generate your first image to get started.",
    "allStyles": "All Styles",
    "usageTabs": {
      "all": "All",
      "articleCover": "Article Cover",
      "socialPost": "Social Post",
      "brandAsset": "Brand Asset",
      "custom": "Custom"
    },
    "generateModal": {
      "title": "Generate Image",
      "subtitle": "AI-generated visuals for your content",
      "usage": "Usage",
      "style": "Style",
      "quality": "Quality",
      "prompt": "Prompt",
      "imageTitle": "Title",
      "keyword": "Keyword",
      "linkToArticle": "Link to article",
      "optional": "optional",
      "autoGenerate": "Auto-generate from title+keyword",
      "titlePlaceholder": "Article or post title",
      "keywordPlaceholder": "Target keyword",
      "none": "None",
      "cancel": "Cancel",
      "generate": "Generate",
      "generating": "Generating image…",
      "generatingHint": "This may take a few seconds.",
      "priceStandard": "from $0.04",
      "priceHD": "from $0.17",
      "hd": "HD",
      "standard": "Standard"
    },
    "attachModal": {
      "title": "Attach Image",
      "subtitle": "Link this image to an article or post",
      "attachTo": "Attach to",
      "selectArticle": "Select article",
      "selectSocialPost": "Select social post",
      "choose": "— choose —",
      "attaching": "Attaching…",
      "attach": "Attach"
    },
    "card": {
      "attach": "Attach",
      "copyUrl": "Copy URL",
      "delete": "Delete",
      "free": "free"
    }
  },
  "social": {
    "title": "Social Studio",
    "subtitle": "AI-crafted posts for every platform.",
    "newPost": "New Post",
    "platformTabs": {
      "all": "All",
      "linkedin": "LinkedIn",
      "twitter": "Twitter",
      "instagram": "Instagram",
      "facebook": "Facebook"
    },
    "noPosts": "No posts yet",
    "noPostsHint": "Create your first post to get started.",
    "createPost": "Create Post",
    "newPostModal": {
      "title": "New Post",
      "subtitle": "Compose or generate a social post",
      "platform": "Platform",
      "postType": "Post type",
      "sourceArticle": "Source article",
      "optional": "optional",
      "none": "None",
      "cancel": "Cancel",
      "generateWithAI": "Generate with AI",
      "writeManually": "Write Manually",
      "createPost": "Create Post",
      "back": "Back",
      "generating": "Generating post…",
      "generatingHint": "AI is crafting your post for {{platform}}."
    },
    "editDrawer": {
      "content": "Content",
      "hashtags": "Hashtags",
      "schedule": "Schedule",
      "saving": "Saving…",
      "saved": "Saved",
      "cancel": "Cancel",
      "save": "Save"
    },
    "card": {
      "edit": "Edit",
      "generate": "Generate",
      "generating": "Generating…",
      "schedule": "Schedule",
      "publish": "Publish",
      "publishing": "Publishing…",
      "delete": "Delete"
    },
    "postTypes": {
      "articleShare": "Article Share",
      "tip": "Tip",
      "question": "Question",
      "announcement": "Announcement"
    }
  },
  "content": {
    "title": "Content Planner",
    "subtitle": "Plan and manage your content pipeline.",
    "newItem": "+ New Item",
    "generatePlan": "Generate Plan",
    "generating": "Generating...",
    "noContent": "No content plan yet",
    "noContentHint": "Generate your first plan to start building your content pipeline.",
    "generateFirst": "Generate your first plan",
    "loadingPlans": "Loading plans…",
    "loadError": "Failed to load content plans. Please try again.",
    "views": {
      "kanban": "Kanban",
      "list": "List"
    },
    "noItems": "No items",
    "contentItem": "Content Item",
    "statuses": {
      "idea": "Idea",
      "draft": "Draft",
      "inReview": "In Review",
      "approved": "Approved",
      "published": "Published"
    },
    "types": {
      "article": "Article",
      "landingPage": "Landing Page",
      "socialPost": "Social Post",
      "email": "Email"
    },
    "detailDrawer": {
      "titleLabel": "Title",
      "status": "Status",
      "type": "Type",
      "keyword": "Target Keyword",
      "scheduledDate": "Scheduled Date",
      "wordCount": "Word Count Target",
      "notes": "Notes",
      "notesPlaceholder": "Add notes...",
      "deleting": "Deleting...",
      "deleteItem": "Delete Item"
    },
    "generateModal": {
      "title": "Generate Content Plan",
      "seedKeyword": "Seed keyword (optional)",
      "seedHint": "Uses your keyword research if available, otherwise uses seed.",
      "cancel": "Cancel",
      "generating": "Generating...",
      "generate": "Generate"
    },
    "newItemForm": {
      "title": "New Content Item",
      "placeholder": "Enter title...",
      "cancel": "Cancel",
      "adding": "Adding...",
      "add": "Add Item"
    },
    "tableHeaders": {
      "title": "Title",
      "type": "Type",
      "status": "Status",
      "keyword": "Keyword",
      "date": "Date",
      "actions": "Actions"
    },
    "noItemsYet": "No items yet. Add your first item!"
  },
  "audit": {
    "title": "SEO Audit",
    "subtitle": "Crawl your site and surface technical issues, content gaps, and on-page improvements.",
    "runAudit": "Run Audit",
    "running": "Running…",
    "noAudit": "No audit yet",
    "noAuditHint": "Click Run Audit to crawl your site and get a full SEO report.",
    "crawlingPages": "Crawling pages…",
    "analysingSignals": "Analysing SEO signals…",
    "crawl": "Crawl",
    "audit": "Audit",
    "auditFailed": "Audit failed",
    "auditSuccess": "Audit completed successfully",
    "scoreOverview": "Score Overview",
    "scores": {
      "overall": "Overall",
      "technical": "Technical",
      "content": "Content",
      "onPage": "On-Page"
    },
    "stats": {
      "pagesAudited": "Pages Audited",
      "criticalIssues": "Critical Issues",
      "warnings": "Warnings"
    },
    "tableHeaders": {
      "severity": "Severity",
      "issueType": "Issue Type",
      "url": "URL",
      "details": "Details"
    },
    "noIssues": "No issues found",
    "noIssuesHint": "Your site is in great shape — no SEO issues detected.",
    "issuesCount": "Issues ({{n}})"
  },
  "backlinks": {
    "title": "Backlinks",
    "subtitle": "Monitor your link profile, find opportunities, and exchange links.",
    "tabs": {
      "profile": "Profile",
      "backlinks": "Backlinks",
      "opportunities": "Opportunities",
      "exchange": "Exchange"
    },
    "noData": "Run your first backlink analysis to get started.",
    "analyze": "Analyze Backlinks",
    "reanalyze": "Re-analyze",
    "syncing": "Syncing…",
    "lastSynced": "Last synced {{date}}",
    "neverSynced": "Never synced",
    "stats": {
      "totalBacklinks": "Total Backlinks",
      "referringDomains": "Referring Domains",
      "domainAuthority": "Domain Authority",
      "trustScore": "Trust Score",
      "spamScore": "Spam Score"
    },
    "tableHeaders": {
      "sourceDomain": "Source Domain",
      "anchor": "Anchor",
      "da": "DA",
      "type": "Type",
      "domain": "Domain",
      "linksTo": "Links To",
      "status": "Status"
    },
    "hideSpam": "Hide spam",
    "noBacklinks": "No backlinks found yet. Run analysis first.",
    "statuses": {
      "new": "new",
      "contacted": "contacted",
      "won": "won",
      "lost": "lost",
      "ignored": "ignored"
    },
    "exchange": {
      "yourListing": "Your Listing",
      "listYourSite": "List Your Site",
      "siteUrl": "Site URL",
      "niche": "Niche (e.g. tech, finance)",
      "language": "Language (e.g. en)",
      "description": "Description",
      "save": "Save",
      "listMySite": "List My Site",
      "deactivate": "Deactivate",
      "board": "Board",
      "myRequests": "My Requests",
      "requestExchange": "Request Exchange",
      "noListings": "No listings available yet.",
      "yourLinkUrl": "Your link URL",
      "theirLinkUrl": "Their link URL",
      "message": "Message (optional)",
      "send": "Send",
      "cancel": "Cancel"
    },
    "requests": {
      "sent": "Sent",
      "received": "Received",
      "noRequests": "No {{role}} requests.",
      "accept": "Accept",
      "reject": "Reject",
      "cancel": "Cancel",
      "verify": "Verify",
      "requesterLink": "Requester link",
      "targetLink": "Target link"
    }
  },
  "publishing": {
    "title": "Publishing",
    "subtitle": "Connect your CMS and publish articles directly.",
    "addConnection": "Add Connection",
    "connections": "Connections",
    "noConnections": "No publishing connections yet",
    "noConnectionsHint": "Add a connection to start publishing articles to your CMS.",
    "publishHistory": "Publish History",
    "noJobs": "No publish jobs yet.",
    "editConnection": "Edit Connection",
    "connectionModal": {
      "addTitle": "Add Connection",
      "editTitle": "Edit Connection",
      "addSubtitle": "Connect your CMS to publish articles directly.",
      "editSubtitle": "Update your CMS connection details.",
      "name": "Name",
      "platform": "Platform",
      "siteUrl": "Site URL",
      "username": "WordPress username",
      "password": "Application password",
      "passwordHint": "(leave blank to keep unchanged)",
      "usernamePlaceholder": "admin",
      "cancel": "Cancel",
      "saving": "Saving…",
      "adding": "Adding…",
      "saveChanges": "Save Changes",
      "addConnection": "Add Connection"
    },
    "connectionCard": {
      "connected": "Connected",
      "failed": "Failed",
      "notTested": "Not tested",
      "lastTested": "Last tested:",
      "connectionSuccessful": "Connection successful",
      "connectionFailed": "Connection failed",
      "testing": "Testing…",
      "test": "Test",
      "deactivate": "Deactivate",
      "activate": "Activate",
      "edit": "Edit",
      "delete": "Delete"
    },
    "job": {
      "viewPost": "View post",
      "unknownArticle": "Unknown article",
      "unknownConnection": "Unknown connection"
    }
  },
  "brandVoice": {
    "title": "Brand Voice",
    "subtitle": "Define how your brand communicates across all generated content.",
    "newVoice": "New Voice",
    "loadingVoices": "Loading voices…",
    "loadError": "Failed to load brand voices. Please try again.",
    "noVoices": "No brand voices yet",
    "noVoicesHint": "Create your first brand voice to define how your brand communicates.",
    "createFirst": "Create your first brand voice",
    "default": "Default",
    "edit": "Edit",
    "setDefault": "Set Default",
    "detailPanel": {
      "voiceSettings": "Voice Settings",
      "basicSettings": "Basic Settings",
      "voiceCharacteristics": "Voice Characteristics",
      "trainingSources": "Training Sources",
      "generatedPrompt": "Generated Voice Prompt",
      "name": "Name",
      "tone": "Tone",
      "description": "Description",
      "preferredWords": "Preferred words",
      "wordsToAvoid": "Words to avoid",
      "noTrainingSources": "No training sources yet.",
      "typeAndEnter": "Type a word and press Enter",
      "urlType": "URL",
      "textType": "Text",
      "addSource": "Add Source",
      "adding": "Adding…",
      "noPrompt": "No voice prompt generated yet. Train on sources or generate one now.",
      "regenerating": "Regenerating…",
      "regenerate": "Regenerate",
      "generating": "Generating…",
      "generatePrompt": "Generate Voice Prompt",
      "loading": "Loading…"
    },
    "createModal": {
      "title": "New Brand Voice",
      "name": "Name",
      "tone": "Tone",
      "description": "Description",
      "optional": "optional",
      "descPlaceholder": "Describe this voice...",
      "cancel": "Cancel",
      "creating": "Creating…",
      "create": "Create"
    },
    "tones": {
      "professional": "Professional",
      "conversational": "Conversational",
      "authoritative": "Authoritative",
      "friendly": "Friendly",
      "technical": "Technical",
      "inspirational": "Inspirational"
    }
  },
  "settings": {
    "language": "Language",
    "languageDescription": "Choose the language for the dashboard interface.",
    "account": {
      "title": "Account",
      "subtitle": "Your personal profile and membership details.",
      "fullName": "Full name",
      "email": "Email",
      "role": "Role",
      "memberSince": "Member since"
    },
    "organization": {
      "title": "Organization",
      "subtitle": "Details about your organization and current plan.",
      "name": "Organization name",
      "slug": "Slug",
      "plan": "Plan",
      "upgrade": "Upgrade →"
    },
    "aiKeys": {
      "title": "AI Keys",
      "subtitle": "Add your own API keys. Fennex uses whichever providers you've connected.",
      "connected": "Connected",
      "notConnected": "Not connected",
      "addKey": "Add API key",
      "saveKey": "Save key",
      "addKeyAction": "Add key",
      "saveError": "Failed to save key.",
      "saving": "Saving…"
    },
    "socialAccounts": {
      "title": "Social Accounts",
      "subtitle": "Connect your social accounts to enable direct publishing from the Social Studio.",
      "notConnected": "Not connected",
      "connect": "Connect",
      "cancel": "Cancel",
      "disconnect": "Disconnect",
      "connectAccount": "Connect account",
      "tokenHint": "Paste your access token from the {{platform}} developer portal.",
      "connectError": "Failed to connect."
    },
    "team": {
      "title": "Team",
      "memberCount": "{{n}} member(s) in your organization.",
      "invite": "Invite member",
      "inviteTitle": "Invite team member",
      "generateLink": "Generate invite link",
      "generating": "Generating…",
      "done": "Done",
      "cancel": "Cancel",
      "linkReady": "Invite link ready",
      "linkHint": "Share this link with your team member. It expires in 7 days.",
      "copied": "Copied",
      "copy": "Copy",
      "you": "You",
      "inactive": "Inactive",
      "deactivate": "Deactivate"
    },
    "billing": {
      "currentPlan": "Current plan",
      "usageThisMonth": "Usage this month",
      "plans": "Plans",
      "monthly": "Monthly",
      "annual": "Annual",
      "annualDiscount": "−20%",
      "trialEnds": "Trial ends in {{n}} day(s)",
      "managePlan": "Manage plan →",
      "opening": "Opening…",
      "upgrade": "Upgrade →",
      "downgrade": "Downgrade",
      "resources": {
        "articles": "Articles",
        "images": "Images",
        "social": "Social posts",
        "keywords": "Keywords tracked",
        "brandVoices": "Brand voices",
        "audits": "Audit runs",
        "backlinks": "Backlink analyses"
      }
    }
  },
  "billing": {
    "limitReached": "Limit reached",
    "limitUsed": "You've used {{used}}/{{limit}} {{label}} on your current plan.",
    "upgradeTo": "Upgrade to {{name}} to keep going.",
    "contactEnterprise": "Contact us for Enterprise options.",
    "upgradeCta": "Upgrade to {{name}} — ${{price}}/mo →",
    "redirecting": "Redirecting…",
    "maybeLater": "Maybe later",
    "atLimit": "You've reached your {{label}} limit ({{used}}/{{limit}}).",
    "nearLimit": "You've used {{used}}/{{limit}} {{label}} this month.",
    "upgradeToContinue": "Upgrade to continue →"
  }
}
```

- [ ] **Step 3: Verify the JSON is valid**

```bash
node -e "JSON.parse(require('fs').readFileSync('apps/web/public/locales/en/common.json', 'utf8')); console.log('JSON valid')"
```

Expected: `JSON valid`

- [ ] **Step 4: Commit**

```bash
git add apps/web/public/locales/en/common.json
git commit -m "feat(web): add complete English translation file (en/common.json)"
```

---

### Task 4: `LanguagePicker` component + TopBar + Settings integration

**Files:**
- Create: `apps/web/components/layout/LanguagePicker.tsx`
- Modify: `apps/web/components/layout/TopBar.tsx` (add LanguagePicker, apply `t()` to all strings)
- Modify: `apps/web/app/(dashboard)/settings/page.tsx` (add Language row in Account section, apply `t()` to all strings)

**Interfaces:**
- Consumes: `SUPPORTED_LOCALES`, `Locale` from `@/lib/i18n`; `updateMyLanguage` from `@/lib/api`; `useTranslation` from `react-i18next`
- Produces: `LanguagePicker` component with `showLabel?: boolean` prop

- [ ] **Step 1: Create `apps/web/components/layout/LanguagePicker.tsx`**

```typescript
"use client";
import { useTranslation } from "react-i18next";
import { useMutation, useQueryClient } from "@tanstack/react-query";
import { SUPPORTED_LOCALES, Locale } from "@/lib/i18n";
import { updateMyLanguage } from "@/lib/api";
import { isAuthenticated } from "@/lib/api";

const LOCALE_META: Record<Locale, { label: string; flag: string }> = {
  en: { label: "English",   flag: "🇬🇧" },
  fr: { label: "Français",  flag: "🇫🇷" },
  es: { label: "Español",   flag: "🇪🇸" },
  de: { label: "Deutsch",   flag: "🇩🇪" },
  pt: { label: "Português", flag: "🇵🇹" },
  ar: { label: "العربية",   flag: "🇸🇦" },
};

interface LanguagePickerProps {
  showLabel?: boolean;
}

export function LanguagePicker({ showLabel = false }: LanguagePickerProps) {
  const { i18n } = useTranslation();
  const queryClient = useQueryClient();
  const current = (i18n.language?.slice(0, 2) ?? "en") as Locale;
  const meta = LOCALE_META[current] ?? LOCALE_META.en;

  const mutation = useMutation({
    mutationFn: (lang: Locale) => {
      i18n.changeLanguage(lang);
      if (isAuthenticated()) return updateMyLanguage(lang);
      return Promise.resolve({ language: lang });
    },
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ["me"] });
    },
  });

  return (
    <div className="relative group">
      <button
        className="flex items-center gap-1.5 rounded-lg px-2 py-1.5 text-sm text-muted-foreground hover:text-foreground hover:bg-accent transition-colors"
        aria-label="Language"
      >
        <span className="text-base leading-none">{meta.flag}</span>
        {showLabel && <span className="font-medium">{meta.label}</span>}
      </button>
      <div className="absolute right-0 top-full mt-1 z-50 hidden group-hover:block w-40 rounded-xl border border-border bg-popover shadow-lg py-1">
        {SUPPORTED_LOCALES.map((lang) => {
          const m = LOCALE_META[lang];
          return (
            <button
              key={lang}
              onClick={() => mutation.mutate(lang)}
              className={`w-full flex items-center gap-2.5 px-3 py-2 text-sm text-left hover:bg-accent transition-colors ${
                lang === current ? "text-foreground font-medium" : "text-muted-foreground"
              }`}
            >
              <span className="text-base">{m.flag}</span>
              <span>{m.label}</span>
            </button>
          );
        })}
      </div>
    </div>
  );
}
```

- [ ] **Step 2: Apply `t()` to `TopBar.tsx` and add `LanguagePicker`**

Add `useTranslation` and `LanguagePicker` import at the top of `TopBar.tsx`:

```typescript
import { useTranslation } from "react-i18next";
import { LanguagePicker } from "@/components/layout/LanguagePicker";
```

Inside the component, add:

```typescript
const { t } = useTranslation();
```

Replace all hardcoded strings:
- `"Search or jump to…"` → `t("topbar.searchPlaceholder")`
- `"Notifications"` → `t("topbar.notifications")`
- `"All caught up"` → `t("topbar.allCaughtUp")`
- `"You're all caught up"` → `t("topbar.allCaughtUpDesc")`
- `"Job completions and alerts will show up here."` → `t("topbar.notificationsHint")`
- `"Settings"` → `t("nav.settings")`
- `"Brand Voice"` → `t("nav.brandVoice")`
- `"Sign out"` → `t("nav.signOut")`
- `"Loading…"` → `t("topbar.loading")`

Add `<LanguagePicker />` in the right-side controls area of the TopBar, just before the notifications button or profile menu (wherever the icon buttons cluster is).

- [ ] **Step 3: Apply `t()` to `settings/page.tsx` and add Language row**

Add `useTranslation` import:

```typescript
import { useTranslation } from "react-i18next";
import { LanguagePicker } from "@/components/layout/LanguagePicker";
```

Add at top of the component:

```typescript
const { t } = useTranslation();
```

Add the Language row in the Account section, after the "Member since" row and before the closing `</div>` of that section:

```tsx
<div className="flex items-center justify-between py-3 border-t border-border">
  <div>
    <p className="text-sm font-medium">{t("settings.language")}</p>
    <p className="text-xs text-muted-foreground">{t("settings.languageDescription")}</p>
  </div>
  <LanguagePicker showLabel />
</div>
```

Replace all hardcoded strings in settings using the `settings.*` keys from `en/common.json`. Key mappings:

| Hardcoded | Key |
|-----------|-----|
| `"Account"` (section title) | `t("settings.account.title")` |
| `"Your personal profile and membership details."` | `t("settings.account.subtitle")` |
| `"Full name"` | `t("settings.account.fullName")` |
| `"Email"` | `t("settings.account.email")` |
| `"Role"` | `t("settings.account.role")` |
| `"Member since"` | `t("settings.account.memberSince")` |
| `"Organization"` (section title) | `t("settings.organization.title")` |
| `"Details about your organization and current plan."` | `t("settings.organization.subtitle")` |
| `"Organization name"` | `t("settings.organization.name")` |
| `"Slug"` | `t("settings.organization.slug")` |
| `"Plan"` | `t("settings.organization.plan")` |
| `"Upgrade →"` (in org section) | `t("settings.organization.upgrade")` |
| `"AI Keys"` | `t("settings.aiKeys.title")` |
| `"Add your own API keys…"` | `t("settings.aiKeys.subtitle")` |
| `"Connected"` | `t("settings.aiKeys.connected")` |
| `"Not connected"` | `t("settings.aiKeys.notConnected")` |
| `"Add API key"` | `t("settings.aiKeys.addKey")` |
| `"Save key"` | `t("settings.aiKeys.saveKey")` |
| `"Add key"` | `t("settings.aiKeys.addKeyAction")` |
| `"Failed to save key."` | `t("settings.aiKeys.saveError")` |
| `"Saving…"` | `t("settings.aiKeys.saving")` |
| `"Social Accounts"` | `t("settings.socialAccounts.title")` |
| `"Connect your social accounts…"` | `t("settings.socialAccounts.subtitle")` |
| `"Not connected"` (social) | `t("settings.socialAccounts.notConnected")` |
| `"Connect"` | `t("settings.socialAccounts.connect")` |
| `"Cancel"` | `t("settings.socialAccounts.cancel")` |
| `"Disconnect"` | `t("settings.socialAccounts.disconnect")` |
| `"Connect account"` | `t("settings.socialAccounts.connectAccount")` |
| `"Paste your access token from the {platform} developer portal."` | `t("settings.socialAccounts.tokenHint", { platform })` |
| `"Failed to connect."` | `t("settings.socialAccounts.connectError")` |
| `"Team"` | `t("settings.team.title")` |
| `"{n} member(s) in your organization."` | `t("settings.team.memberCount", { n: members.length })` |
| `"Invite member"` | `t("settings.team.invite")` |
| `"Invite team member"` | `t("settings.team.inviteTitle")` |
| `"Generate invite link"` | `t("settings.team.generateLink")` |
| `"Generating…"` | `t("settings.team.generating")` |
| `"Done"` | `t("settings.team.done")` |
| `"Cancel"` | `t("settings.team.cancel")` |
| `"Invite link ready"` | `t("settings.team.linkReady")` |
| `"Share this link with your team member. It expires in 7 days."` | `t("settings.team.linkHint")` |
| `"Copied"` | `t("settings.team.copied")` |
| `"Copy"` | `t("settings.team.copy")` |
| `"You"` | `t("settings.team.you")` |
| `"Inactive"` | `t("settings.team.inactive")` |
| `"Deactivate"` | `t("settings.team.deactivate")` |
| `"Current plan"` (billing) | `t("settings.billing.currentPlan")` |
| `"Usage this month"` | `t("settings.billing.usageThisMonth")` |
| `"Plans"` | `t("settings.billing.plans")` |
| `"Monthly"` | `t("settings.billing.monthly")` |
| `"Annual"` | `t("settings.billing.annual")` |
| `"−20%"` | `t("settings.billing.annualDiscount")` |
| `"Trial ends in {n} day(s)"` | `t("settings.billing.trialEnds", { n })` |
| `"Manage plan →"` | `t("settings.billing.managePlan")` |
| `"Opening…"` | `t("settings.billing.opening")` |
| `"Upgrade →"` (billing) | `t("settings.billing.upgrade")` |
| `"Downgrade"` | `t("settings.billing.downgrade")` |
| `"Current plan"` (badge) | `t("settings.billing.currentPlan")` |
| Resource labels | `t("settings.billing.resources.articles")` etc. |

- [ ] **Step 4: Verify TypeScript compiles**

```bash
cd apps/web && pnpm tsc --noEmit 2>&1 | head -40
```

Expected: no new errors.

- [ ] **Step 5: Commit**

```bash
git add apps/web/components/layout/LanguagePicker.tsx \
        apps/web/components/layout/TopBar.tsx \
        apps/web/app/(dashboard)/settings/page.tsx
git commit -m "feat(web): LanguagePicker component + TopBar and Settings language integration"
```

---

### Task 5: Navigation — `Sidebar.tsx` and `CommandPalette.tsx`

**Files:**
- Modify: `apps/web/components/layout/Sidebar.tsx`
- Modify: `apps/web/components/layout/CommandPalette.tsx`

**Interfaces:**
- Consumes: `useTranslation` from `react-i18next`

- [ ] **Step 1: Apply `t()` to `Sidebar.tsx`**

Add import:

```typescript
import { useTranslation } from "react-i18next";
```

Add inside component:

```typescript
const { t } = useTranslation();
```

Replace all hardcoded nav labels:

| Hardcoded | Key |
|-----------|-----|
| `"Research"` (group) | `t("nav.research")` |
| `"Create"` (group) | `t("nav.create")` |
| `"Grow"` (group) | `t("nav.grow")` |
| `"Overview"` | `t("nav.overview")` |
| `"Keywords"` | `t("nav.keywords")` |
| `"Planner"` | `t("nav.planner")` |
| `"Articles"` | `t("nav.articles")` |
| `"Social"` | `t("nav.social")` |
| `"Images"` | `t("nav.images")` |
| `"Publishing"` | `t("nav.publishing")` |
| `"Backlinks"` | `t("nav.backlinks")` |
| `"Analytics"` | `t("nav.analytics")` |
| `"Audit"` | `t("nav.audit")` |
| `"Brand Voice"` (footer) | `t("nav.brandVoice")` |
| `"Settings"` (footer) | `t("nav.settings")` |
| `"Sign out"` (footer) | `t("nav.signOut")` |
| `"Create project"` | `t("nav.createProject")` |
| `"New project"` | `t("nav.newProject")` |
| `"Select project"` | `t("nav.selectProject")` |

- [ ] **Step 2: Apply `t()` to `CommandPalette.tsx`**

`CommandPalette` generates its command list in a `useMemo`. The `t` function must be declared at the component level and added to the `useMemo` dependency array.

Add import:

```typescript
import { useTranslation } from "react-i18next";
```

Add inside component (before the `useMemo`):

```typescript
const { t } = useTranslation();
```

Inside the `NAV` array (which is probably defined outside the component or inside the component), the labels must use `t()`. If `NAV` is a static array outside the component, move it inside the component or convert it to a function that receives `t`. The cleanest approach — convert to a function called inside the `useMemo`:

```typescript
// If NAV is a static const outside the component, replace it with a function inside the component:
const navCommands = useMemo(() => [
  { id: "overview",  label: t("nav.overview"),  href: `/${projectId}/overview`,  group: t("commandPalette.groups.navigate") },
  { id: "keywords",  label: t("nav.keywords"),  href: `/${projectId}/keywords`,  group: t("commandPalette.groups.navigate") },
  { id: "planner",   label: t("nav.planner"),   href: `/${projectId}/content`,   group: t("commandPalette.groups.navigate") },
  { id: "articles",  label: t("nav.articles"),  href: `/${projectId}/articles`,  group: t("commandPalette.groups.navigate") },
  { id: "social",    label: t("nav.social"),    href: `/${projectId}/social`,    group: t("commandPalette.groups.navigate") },
  { id: "images",    label: t("nav.images"),    href: `/${projectId}/images`,    group: t("commandPalette.groups.navigate") },
  { id: "publishing",label: t("nav.publishing"),href: `/${projectId}/publishing`,group: t("commandPalette.groups.navigate") },
  { id: "backlinks", label: t("nav.backlinks"), href: `/${projectId}/backlinks`, group: t("commandPalette.groups.navigate") },
  { id: "analytics", label: t("nav.analytics"), href: `/${projectId}/analytics`, group: t("commandPalette.groups.navigate") },
  { id: "audit",     label: t("nav.audit"),     href: `/${projectId}/audit`,     group: t("commandPalette.groups.navigate") },
  { id: "brandVoice",label: t("nav.brandVoice"),href: "/brand-voice",            group: t("commandPalette.groups.navigate") },
  { id: "settings",  label: t("nav.settings"),  href: "/settings",              group: t("commandPalette.groups.navigate") },
], [t, projectId]);
```

The `commands` useMemo (which adds theme toggle and sign-out actions) also needs `t`:

```typescript
const commands = useMemo(() => [
  ...navCommands,
  ...projects.map((p) => ({
    id: `project-${p.id}`,
    label: p.name,
    group: t("commandPalette.groups.switchProject"),
    onSelect: () => { /* ... */ },
  })),
  {
    id: "theme-toggle",
    label: isDark ? t("commandPalette.actions.lightMode") : t("commandPalette.actions.darkMode"),
    group: t("commandPalette.groups.actions"),
    onSelect: toggleTheme,
  },
  {
    id: "sign-out",
    label: t("commandPalette.actions.signOut"),
    group: t("commandPalette.groups.actions"),
    onSelect: handleSignOut,
  },
], [navCommands, projects, t, isDark, toggleTheme, handleSignOut]);
```

Replace other hardcoded strings:
- `"Search commands, pages, projects…"` → `t("commandPalette.placeholder")`
- `` `No results for "${query}"` `` → `t("commandPalette.noResults", { query })`
- `"navigate"` (footer hint) → `t("commandPalette.navigateHint")`
- `"select"` (footer hint) → `t("commandPalette.selectHint")`
- `"to toggle"` (footer hint) → `t("commandPalette.toggleHint")`

- [ ] **Step 3: Verify TypeScript compiles**

```bash
cd apps/web && pnpm tsc --noEmit 2>&1 | head -40
```

Expected: no new errors.

- [ ] **Step 4: Commit**

```bash
git add apps/web/components/layout/Sidebar.tsx \
        apps/web/components/layout/CommandPalette.tsx
git commit -m "feat(web): apply t() to Sidebar and CommandPalette nav labels"
```

---

### Task 6: Auth pages

**Files:**
- Modify: `apps/web/app/(auth)/login/page.tsx`
- Modify: `apps/web/app/(auth)/register/page.tsx`

**Interfaces:**
- Consumes: `useTranslation` from `react-i18next`

- [ ] **Step 1: Apply `t()` to `login/page.tsx`**

Add import:

```typescript
import { useTranslation } from "react-i18next";
```

Add inside component:

```typescript
const { t } = useTranslation();
```

Replace strings:

| Hardcoded | Key |
|-----------|-----|
| `"Welcome back"` | `t("auth.welcomeBack")` |
| `"Sign in to your Fennex workspace"` | `t("auth.signInSubtitle")` |
| `"Email"` | `t("auth.email")` |
| `"you@company.com"` (placeholder) | `t("auth.emailPlaceholder")` |
| `"Password"` | `t("auth.password")` |
| `"Sign in →"` | `t("auth.signIn")` |
| `"Signing in…"` | `t("auth.signingIn")` |
| `"New to Fennex?"` | `t("auth.newToFennex")` |
| `"Create a free account →"` | `t("auth.createFreeAccount")` |

- [ ] **Step 2: Apply `t()` to `register/page.tsx`**

Add import and `const { t } = useTranslation()` the same way.

Replace strings:

| Hardcoded | Key |
|-----------|-----|
| `"Create your account"` | `t("auth.createAccount")` |
| `"Free to start · No credit card required"` | `t("auth.registerSubtitle")` |
| `"Full name"` | `t("auth.fullName")` |
| `"Company"` | `t("auth.company")` |
| `"Work email"` | `t("auth.workEmail")` |
| `"Password"` | `t("auth.password")` |
| `"Min. 6 characters"` | `t("auth.passwordHint")` |
| `"Get started for free →"` | `t("auth.getStarted")` |
| `"Creating account…"` | `t("auth.creatingAccount")` |
| `"Have an account?"` | `t("auth.haveAccount")` |
| `"Sign in instead →"` | `t("auth.signInInstead")` |
| Terms text | `t("auth.termsText")` |

- [ ] **Step 3: Verify TypeScript compiles**

```bash
cd apps/web && pnpm tsc --noEmit 2>&1 | head -40
```

Expected: no new errors.

- [ ] **Step 4: Commit**

```bash
git add apps/web/app/\(auth\)/login/page.tsx \
        apps/web/app/\(auth\)/register/page.tsx
git commit -m "feat(web): apply t() to login and register pages"
```

---

### Task 7: Dashboard home + Brand Voice + Billing components

**Files:**
- Modify: `apps/web/app/(dashboard)/page.tsx`
- Modify: `apps/web/app/(dashboard)/brand-voice/page.tsx`
- Modify: `apps/web/components/billing/UpgradeModal.tsx`
- Modify: `apps/web/components/billing/UsageBanner.tsx`

**Interfaces:**
- Consumes: `useTranslation` from `react-i18next`

- [ ] **Step 1: Apply `t()` to `app/(dashboard)/page.tsx`**

Add import and `const { t } = useTranslation()` inside the component.

Replace strings:

| Hardcoded | Key |
|-----------|-----|
| `"Command Center"` | `t("dashboard.commandCenter")` |
| `"Welcome back"` | `t("dashboard.welcomeBack")` |
| `"Project overview"` | `t("dashboard.projectOverview")` |
| `"No projects yet"` | `t("dashboard.noProjects")` |
| `"Welcome to Fennex"` | `t("dashboard.welcomeToFennex")` |
| `"Your AI SEO growth platform."` | `t("dashboard.tagline")` |
| `"Organic clicks · 28d"` | `t("dashboard.organicClicks")` |
| `"Connect Search Console to see traffic trends."` | `t("dashboard.connectSearchConsole")` |
| `"Setup"` | `t("dashboard.setup")` |
| `"complete"` | `t("dashboard.complete")` |
| `"Quick actions"` | `t("dashboard.quickActions")` |
| `"Keyword research"` | `t("dashboard.keywordResearch")` |
| `"Generate article"` | `t("dashboard.generateArticle")` |
| `"Audit your site"` | `t("dashboard.auditSite")` |
| `"Recent articles"` | `t("dashboard.recentArticles")` |
| `"No articles yet"` | `t("dashboard.noArticlesYet")` |
| `"Generate your first article →"` | `t("dashboard.generateFirstArticle")` |
| `"Select a project."` | `t("dashboard.selectProject")` |
| `"Impressions"` | `t("dashboard.stats.impressions")` |
| `"Avg CTR"` | `t("dashboard.stats.avgCtr")` |
| `"Avg Position"` | `t("dashboard.stats.avgPosition")` |
| `"Published"` | `t("dashboard.stats.published")` |
| `"Create a project"` | `t("dashboard.setupSteps.createProject")` |
| `"Connect AI keys"` | `t("dashboard.setupSteps.connectKeys")` |
| `"Connect Search Console"` (setup step) | `t("dashboard.setupSteps.connectSearchConsole")` |
| `"Generate an article"` | `t("dashboard.setupSteps.generateArticle")` |
| `"View all →"` | `t("common.viewAll")` |

- [ ] **Step 2: Apply `t()` to `brand-voice/page.tsx`**

Add import and `const { t } = useTranslation()` inside the component.

Replace strings using `brandVoice.*` keys — full mapping:

| Hardcoded | Key |
|-----------|-----|
| `"Brand Voice"` | `t("brandVoice.title")` |
| `"Define how your brand communicates across all generated content."` | `t("brandVoice.subtitle")` |
| `"New Voice"` | `t("brandVoice.newVoice")` |
| `"Loading voices…"` | `t("brandVoice.loadingVoices")` |
| `"Failed to load brand voices. Please try again."` | `t("brandVoice.loadError")` |
| `"No brand voices yet"` | `t("brandVoice.noVoices")` |
| `"Create your first brand voice to define how your brand communicates."` | `t("brandVoice.noVoicesHint")` |
| `"Create your first brand voice"` | `t("brandVoice.createFirst")` |
| `"Default"` | `t("brandVoice.default")` |
| `"Edit"` | `t("brandVoice.edit")` |
| `"Set Default"` | `t("brandVoice.setDefault")` |
| `"Voice Settings"` | `t("brandVoice.detailPanel.voiceSettings")` |
| `"Basic Settings"` | `t("brandVoice.detailPanel.basicSettings")` |
| `"Voice Characteristics"` | `t("brandVoice.detailPanel.voiceCharacteristics")` |
| `"Training Sources"` | `t("brandVoice.detailPanel.trainingSources")` |
| `"Generated Voice Prompt"` | `t("brandVoice.detailPanel.generatedPrompt")` |
| `"Name"` | `t("brandVoice.detailPanel.name")` |
| `"Tone"` | `t("brandVoice.detailPanel.tone")` |
| `"Description"` | `t("brandVoice.detailPanel.description")` |
| `"Preferred words"` | `t("brandVoice.detailPanel.preferredWords")` |
| `"Words to avoid"` | `t("brandVoice.detailPanel.wordsToAvoid")` |
| `"No training sources yet."` | `t("brandVoice.detailPanel.noTrainingSources")` |
| `"Type a word and press Enter"` | `t("brandVoice.detailPanel.typeAndEnter")` |
| `"URL"` | `t("brandVoice.detailPanel.urlType")` |
| `"Text"` | `t("brandVoice.detailPanel.textType")` |
| `"Add Source"` | `t("brandVoice.detailPanel.addSource")` |
| `"Adding…"` | `t("brandVoice.detailPanel.adding")` |
| `"No voice prompt generated yet…"` | `t("brandVoice.detailPanel.noPrompt")` |
| `"Regenerating…"` | `t("brandVoice.detailPanel.regenerating")` |
| `"Regenerate"` | `t("brandVoice.detailPanel.regenerate")` |
| `"Generating…"` | `t("brandVoice.detailPanel.generating")` |
| `"Generate Voice Prompt"` | `t("brandVoice.detailPanel.generatePrompt")` |
| `"Loading…"` | `t("brandVoice.detailPanel.loading")` |
| `"New Brand Voice"` (modal title) | `t("brandVoice.createModal.title")` |
| `"Name"` (modal) | `t("brandVoice.createModal.name")` |
| `"Tone"` (modal) | `t("brandVoice.createModal.tone")` |
| `"Description"` (modal) | `t("brandVoice.createModal.description")` |
| `"optional"` | `t("brandVoice.createModal.optional")` |
| `"Describe this voice..."` | `t("brandVoice.createModal.descPlaceholder")` |
| `"Cancel"` | `t("brandVoice.createModal.cancel")` |
| `"Creating…"` | `t("brandVoice.createModal.creating")` |
| `"Create"` | `t("brandVoice.createModal.create")` |
| Tone labels | `t("brandVoice.tones.professional")` etc. |

- [ ] **Step 3: Apply `t()` to `UpgradeModal.tsx`**

Add import and `const { t } = useTranslation()` inside the component.

Replace strings:

```typescript
// Replace the hardcoded text with t() calls:
<h2 className="font-display text-xl font-bold">{t("billing.limitReached")}</h2>
<p className="mt-2 text-sm text-muted-foreground">
  {t("billing.limitUsed", { used, limit, label })}
  {" "}
  {next
    ? t("billing.upgradeTo", { name: next.name })
    : t("billing.contactEnterprise")}
</p>
// Button:
{checkoutMutation.isPending
  ? t("billing.redirecting")
  : t("billing.upgradeCta", { name: next.name, price: next.price })}
// Cancel button:
{t("billing.maybeLater")}
```

- [ ] **Step 4: Apply `t()` to `UsageBanner.tsx`**

Add import and `const { t } = useTranslation()` inside the component.

Replace strings:

```typescript
{isAtLimit
  ? t("billing.atLimit", { label, used, limit })
  : t("billing.nearLimit", { used, limit, label })}
{" "}
<button onClick={onUpgrade} className="underline underline-offset-2 font-medium">
  {t("billing.upgradeToContinue")}
</button>
```

- [ ] **Step 5: Verify TypeScript compiles**

```bash
cd apps/web && pnpm tsc --noEmit 2>&1 | head -40
```

Expected: no new errors.

- [ ] **Step 6: Commit**

```bash
git add apps/web/app/\(dashboard\)/page.tsx \
        apps/web/app/\(dashboard\)/brand-voice/page.tsx \
        apps/web/components/billing/UpgradeModal.tsx \
        apps/web/components/billing/UsageBanner.tsx
git commit -m "feat(web): apply t() to dashboard home, brand-voice, and billing components"
```

---

### Task 8: All [projectId] pages

**Files:**
- Modify: `apps/web/app/(dashboard)/[projectId]/overview/page.tsx`
- Modify: `apps/web/app/(dashboard)/[projectId]/articles/page.tsx`
- Modify: `apps/web/app/(dashboard)/[projectId]/keywords/page.tsx`
- Modify: `apps/web/app/(dashboard)/[projectId]/analytics/page.tsx`
- Modify: `apps/web/app/(dashboard)/[projectId]/images/page.tsx`
- Modify: `apps/web/app/(dashboard)/[projectId]/social/page.tsx`
- Modify: `apps/web/app/(dashboard)/[projectId]/content/page.tsx`
- Modify: `apps/web/app/(dashboard)/[projectId]/audit/page.tsx`
- Modify: `apps/web/app/(dashboard)/[projectId]/backlinks/page.tsx`
- Modify: `apps/web/app/(dashboard)/[projectId]/publishing/page.tsx`

**Interfaces:**
- Consumes: `useTranslation` from `react-i18next`

The pattern for every file in this task is identical:

1. Add `import { useTranslation } from "react-i18next";`
2. Add `const { t } = useTranslation();` at the top of the component
3. Replace each hardcoded string with the matching `t("section.key")` call

The key namespace for each file maps to the matching top-level key in `en/common.json`.

- [ ] **Step 1: Apply `t()` to `overview/page.tsx`**

Namespace: `overview.*`

| Hardcoded | Key |
|-----------|-----|
| `"Project Overview"` | `t("overview.title")` |
| `"Your organic growth at a glance."` | `t("overview.subtitle")` |
| `"New article"` | `t("overview.newArticle")` |
| `"Last 28 days"` | `t("overview.last28Days")` |
| `"No analytics data yet."` | `t("overview.noAnalytics")` |
| `"Recent Articles"` | `t("overview.recentArticles")` |
| `"View all →"` | `t("overview.viewAll")` |
| `"No articles yet."` | `t("overview.noArticles")` |
| `"Create one"` | `t("overview.createOne")` |
| `"Quick Actions"` | `t("overview.quickActions")` |
| `"Run keyword research"` | `t("overview.runKeywordResearch")` |
| `"Create article"` | `t("overview.createArticle")` |
| `"View analytics"` | `t("overview.viewAnalytics")` |
| `"Clicks"` | `t("overview.stats.clicks")` |
| `"Impressions"` | `t("overview.stats.impressions")` |
| `"Avg CTR"` | `t("overview.stats.avgCtr")` |
| `"Avg Position"` | `t("overview.stats.avgPosition")` |

- [ ] **Step 2: Apply `t()` to `articles/page.tsx`**

Namespace: `articles.*`

| Hardcoded | Key |
|-----------|-----|
| `"Articles"` | `t("articles.title")` |
| `"AI-generated, SEO-optimized content."` | `t("articles.subtitle")` |
| `"New Article"` | `t("articles.newArticle")` |
| `"All articles"` | `t("articles.allArticles")` |
| `"No articles yet"` | `t("articles.noArticles")` |
| `"Select an article to edit"` | `t("articles.selectArticle")` |
| `"Pick one from the list, or generate a new AI-written draft."` | `t("articles.selectArticleHint")` |
| `"Edit"` (tab) | `t("articles.editor.edit")` |
| `"Preview"` | `t("articles.editor.preview")` |
| `"Save"` | `t("articles.editor.save")` |
| `"Saving…"` | `t("articles.editor.saving")` |
| `"Saved"` | `t("articles.editor.saved")` |
| `"Publish"` | `t("articles.editor.publish")` |
| `"Regenerate"` | `t("articles.editor.regenerate")` |
| `"Regenerating…"` | `t("articles.editor.regenerating")` |
| `"Save Revision"` | `t("articles.editor.saveRevision")` |
| `"Breakdown"` | `t("articles.editor.breakdown")` |
| `"SEO Score"` | `t("articles.editor.seoScore")` |
| `"Meta title"` | `t("articles.editor.metaTitle")` |
| `"Meta description"` | `t("articles.editor.metaDescription")` |
| `"Article title"` | `t("articles.editor.articleTitle")` |
| `"words"` | `t("articles.editor.words")` |
| `"New Article"` (modal) | `t("articles.newArticleModal.title")` |
| `"AI will generate a full draft after creation."` | `t("articles.newArticleModal.hint")` |
| `"Generating your article…"` | `t("articles.newArticleModal.generating")` |
| `"This may take up to a minute. Hang tight."` | `t("articles.newArticleModal.generatingHint")` |
| `"Title"` (modal field) | `t("articles.newArticleModal.titleLabel")` |
| `"Target keyword"` | `t("articles.newArticleModal.keyword")` |
| `"Tone"` | `t("articles.newArticleModal.tone")` |
| `"Word count target"` | `t("articles.newArticleModal.wordCount")` |
| `"Cancel"` | `t("articles.newArticleModal.cancel")` |
| `"Create & Generate"` | `t("articles.newArticleModal.create")` |
| `"Publish Article"` | `t("articles.publishModal.title")` |
| `"Choose connection"` | `t("articles.publishModal.chooseConnection")` |
| `"Loading connections…"` | `t("articles.publishModal.loadingConnections")` |
| `"No connections available."` | `t("articles.publishModal.noConnections")` |
| `"Publish as"` | `t("articles.publishModal.publishAs")` |
| `"Publish Now"` | `t("articles.publishModal.publishNow")` |
| `"Published successfully"` | `t("articles.publishModal.publishedSuccess")` |
| `"View published post"` | `t("articles.publishModal.viewPost")` |
| `"Done"` | `t("articles.publishModal.done")` |
| Card `"Edit"` | `t("articles.card.edit")` |
| Card `"Regenerate"` | `t("articles.card.regenerate")` |
| Card `"Delete"` | `t("articles.card.delete")` |

- [ ] **Step 3: Apply `t()` to `keywords/page.tsx`**

Namespace: `keywords.*`

| Hardcoded | Key |
|-----------|-----|
| `"Keywords"` | `t("keywords.title")` |
| `"Discover high-ROI keyword opportunities and group them into clusters."` | `t("keywords.subtitle")` |
| `"Enter a keyword to start"` | `t("keywords.enterKeyword")` |
| `"We'll research related keywords and group them into clusters."` | `t("keywords.enterKeywordHint")` |
| `"Starting…"` | `t("keywords.starting")` |
| `"Analyzing…"` | `t("keywords.analyzing")` |
| `"Analyze"` | `t("keywords.analyze")` |
| `"Discovering keywords and building clusters"` | `t("keywords.discovering")` |
| `"Keyword"` | `t("keywords.tableHeaders.keyword")` |
| `"Volume"` | `t("keywords.tableHeaders.volume")` |
| `"Difficulty"` | `t("keywords.tableHeaders.difficulty")` |
| `"CPC"` | `t("keywords.tableHeaders.cpc")` |
| `"Intent"` | `t("keywords.tableHeaders.intent")` |
| `"All keywords"` | `t("keywords.allKeywords")` |
| `"Clusters"` | `t("keywords.clusters")` |
| `"Clear filter"` | `t("keywords.clearFilter")` |
| `"Research failed"` | `t("keywords.researchFailed")` |
| `"An unexpected error occurred."` | `t("keywords.unexpectedError")` |

- [ ] **Step 4: Apply `t()` to `analytics/page.tsx`**

Namespace: `analytics.*`

| Hardcoded | Key |
|-----------|-----|
| `"Analytics"` | `t("analytics.title")` |
| `"Search performance, rankings, and content impact."` | `t("analytics.subtitle")` |
| Tab `"Overview"` | `t("analytics.tabs.overview")` |
| Tab `"Rankings"` | `t("analytics.tabs.rankings")` |
| Tab `"Pages & Queries"` | `t("analytics.tabs.pagesQueries")` |
| Tab `"Content"` | `t("analytics.tabs.content")` |
| `"Views"` | `t("analytics.views")` |
| `"Date range"` | `t("analytics.dateRange")` |
| `"Clicks"` | `t("analytics.stats.clicks")` |
| `"Impressions"` | `t("analytics.stats.impressions")` |
| `"Avg CTR"` | `t("analytics.stats.avgCtr")` |
| `"Avg Position"` | `t("analytics.stats.avgPosition")` |
| `"Clicks & Impressions"` | `t("analytics.stats.clicksImpressions")` |
| `"Search Console connected"` | `t("analytics.searchConsoleConnected")` |
| `"Connect Google Search Console to sync real traffic data."` | `t("analytics.connectSearchConsole")` |
| `"Connect"` | `t("analytics.connect")` |
| `"Disconnect"` | `t("analytics.disconnect")` |
| `"Last synced"` | `t("analytics.lastSynced")` |
| `"No analytics data yet."` | `t("analytics.noData")` |
| `"Sort by:"` | `t("analytics.sortBy")` |
| `"Loading rankings…"` | `t("analytics.loadingRankings")` |
| `"No keyword rankings yet. Run keyword research first."` | `t("analytics.noRankings")` |
| `"Top Pages"` | `t("analytics.topPages")` |
| `"Top Queries"` | `t("analytics.topQueries")` |
| `"No page data yet."` | `t("analytics.noPageData")` |
| `"No query data yet."` | `t("analytics.noQueryData")` |
| `"Publish articles to see their performance here."` | `t("analytics.publishArticles")` |
| All table headers | `t("analytics.tableHeaders.*")` |
| `"← Previous"` | `t("common.previousPage")` |
| `"Next →"` | `t("common.nextPage")` |
| `` `Page ${page}` `` | `t("common.page", { n: page })` |

- [ ] **Step 5: Apply `t()` to `images/page.tsx`**

Namespace: `images.*`

| Hardcoded | Key |
|-----------|-----|
| `"Image Studio"` | `t("images.title")` |
| `"AI-generated visuals for articles and social posts."` | `t("images.subtitle")` |
| `"Generate Image"` | `t("images.generate")` |
| `"No images generated yet"` | `t("images.noImages")` |
| `"Generate your first image to get started."` | `t("images.noImagesHint")` |
| `"All Styles"` | `t("images.allStyles")` |
| Usage tabs | `t("images.usageTabs.all")` etc. |
| Modal title `"Generate Image"` | `t("images.generateModal.title")` |
| `"AI-generated visuals for your content"` | `t("images.generateModal.subtitle")` |
| `"Usage"` | `t("images.generateModal.usage")` |
| `"Style"` | `t("images.generateModal.style")` |
| `"Quality"` | `t("images.generateModal.quality")` |
| `"Prompt"` | `t("images.generateModal.prompt")` |
| `"Title"` | `t("images.generateModal.imageTitle")` |
| `"Keyword"` | `t("images.generateModal.keyword")` |
| `"Link to article"` | `t("images.generateModal.linkToArticle")` |
| `"optional"` | `t("images.generateModal.optional")` |
| `"Auto-generate from title+keyword"` | `t("images.generateModal.autoGenerate")` |
| `"Article or post title"` | `t("images.generateModal.titlePlaceholder")` |
| `"Target keyword"` | `t("images.generateModal.keywordPlaceholder")` |
| `"None"` | `t("images.generateModal.none")` |
| `"Cancel"` | `t("images.generateModal.cancel")` |
| `"Generate"` | `t("images.generateModal.generate")` |
| `"Generating image…"` | `t("images.generateModal.generating")` |
| `"This may take a few seconds."` | `t("images.generateModal.generatingHint")` |
| `"from $0.04"` | `t("images.generateModal.priceStandard")` |
| `"from $0.17"` | `t("images.generateModal.priceHD")` |
| `"HD"` | `t("images.generateModal.hd")` |
| `"Standard"` | `t("images.generateModal.standard")` |
| `"Attach Image"` | `t("images.attachModal.title")` |
| `"Link this image to an article or post"` | `t("images.attachModal.subtitle")` |
| `"Attach to"` | `t("images.attachModal.attachTo")` |
| `"Select article"` | `t("images.attachModal.selectArticle")` |
| `"Select social post"` | `t("images.attachModal.selectSocialPost")` |
| `"— choose —"` | `t("images.attachModal.choose")` |
| `"Attaching…"` | `t("images.attachModal.attaching")` |
| `"Attach"` | `t("images.attachModal.attach")` |
| Card `"Attach"` | `t("images.card.attach")` |
| Card `"Copy URL"` | `t("images.card.copyUrl")` |
| Card `"Delete"` | `t("images.card.delete")` |
| Card `"free"` | `t("images.card.free")` |

- [ ] **Step 6: Apply `t()` to `social/page.tsx`**

Namespace: `social.*`

| Hardcoded | Key |
|-----------|-----|
| `"Social Studio"` | `t("social.title")` |
| `"AI-crafted posts for every platform."` | `t("social.subtitle")` |
| `"New Post"` | `t("social.newPost")` |
| Platform tabs | `t("social.platformTabs.all")` etc. |
| `"No posts yet"` | `t("social.noPosts")` |
| `"Create your first post to get started."` | `t("social.noPostsHint")` |
| `"Create Post"` | `t("social.createPost")` |
| Modal `"New Post"` | `t("social.newPostModal.title")` |
| `"Compose or generate a social post"` | `t("social.newPostModal.subtitle")` |
| `"Platform"` | `t("social.newPostModal.platform")` |
| `"Post type"` | `t("social.newPostModal.postType")` |
| `"Source article"` | `t("social.newPostModal.sourceArticle")` |
| `"optional"` | `t("social.newPostModal.optional")` |
| `"None"` | `t("social.newPostModal.none")` |
| `"Cancel"` | `t("social.newPostModal.cancel")` |
| `"Generate with AI"` | `t("social.newPostModal.generateWithAI")` |
| `"Write Manually"` | `t("social.newPostModal.writeManually")` |
| `"Create Post"` (button) | `t("social.newPostModal.createPost")` |
| `"Back"` | `t("social.newPostModal.back")` |
| `"Generating post…"` | `t("social.newPostModal.generating")` |
| `` `AI is crafting your post for ${platform}.` `` | `t("social.newPostModal.generatingHint", { platform })` |
| Drawer tabs | `t("social.editDrawer.content")` etc. |
| `"Saving…"` | `t("social.editDrawer.saving")` |
| `"Saved"` | `t("social.editDrawer.saved")` |
| `"Save"` | `t("social.editDrawer.save")` |
| `"Cancel"` (drawer) | `t("social.editDrawer.cancel")` |
| Card actions | `t("social.card.edit")` etc. |
| Post type labels | `t("social.postTypes.articleShare")` etc. |

- [ ] **Step 7: Apply `t()` to `content/page.tsx`**

Namespace: `content.*`

| Hardcoded | Key |
|-----------|-----|
| `"Content Planner"` | `t("content.title")` |
| `"Plan and manage your content pipeline."` | `t("content.subtitle")` |
| `"+ New Item"` | `t("content.newItem")` |
| `"Generate Plan"` | `t("content.generatePlan")` |
| `"Generating..."` | `t("content.generating")` |
| `"No content plan yet"` | `t("content.noContent")` |
| `"Generate your first plan to start building your content pipeline."` | `t("content.noContentHint")` |
| `"Generate your first plan"` | `t("content.generateFirst")` |
| `"Loading plans…"` | `t("content.loadingPlans")` |
| `"Failed to load content plans. Please try again."` | `t("content.loadError")` |
| `"Kanban"` | `t("content.views.kanban")` |
| `"List"` | `t("content.views.list")` |
| `"No items"` | `t("content.noItems")` |
| `"Content Item"` | `t("content.contentItem")` |
| Status labels | `t("content.statuses.idea")` etc. |
| Type labels | `t("content.types.article")` etc. |
| Drawer `"Title"` | `t("content.detailDrawer.titleLabel")` |
| Drawer `"Status"` | `t("content.detailDrawer.status")` |
| Drawer `"Type"` | `t("content.detailDrawer.type")` |
| `"Target Keyword"` | `t("content.detailDrawer.keyword")` |
| `"Scheduled Date"` | `t("content.detailDrawer.scheduledDate")` |
| `"Word Count Target"` | `t("content.detailDrawer.wordCount")` |
| `"Notes"` | `t("content.detailDrawer.notes")` |
| `"Add notes..."` | `t("content.detailDrawer.notesPlaceholder")` |
| `"Deleting..."` | `t("content.detailDrawer.deleting")` |
| `"Delete Item"` | `t("content.detailDrawer.deleteItem")` |
| `"Generate Content Plan"` | `t("content.generateModal.title")` |
| `"Seed keyword (optional)"` | `t("content.generateModal.seedKeyword")` |
| `"Uses your keyword research if available, otherwise uses seed."` | `t("content.generateModal.seedHint")` |
| `"Cancel"` | `t("content.generateModal.cancel")` |
| `"Generate"` | `t("content.generateModal.generate")` |
| `"New Content Item"` | `t("content.newItemForm.title")` |
| `"Enter title..."` | `t("content.newItemForm.placeholder")` |
| `"Adding..."` | `t("content.newItemForm.adding")` |
| `"Add Item"` | `t("content.newItemForm.add")` |
| Table headers | `t("content.tableHeaders.title")` etc. |
| `"No items yet. Add your first item!"` | `t("content.noItemsYet")` |

- [ ] **Step 8: Apply `t()` to `audit/page.tsx`**

Namespace: `audit.*`

| Hardcoded | Key |
|-----------|-----|
| `"SEO Audit"` | `t("audit.title")` |
| `"Crawl your site and surface technical issues…"` | `t("audit.subtitle")` |
| `"Run Audit"` | `t("audit.runAudit")` |
| `"Running…"` | `t("audit.running")` |
| `"No audit yet"` | `t("audit.noAudit")` |
| `"Click Run Audit to crawl your site and get a full SEO report."` | `t("audit.noAuditHint")` |
| `"Crawling pages…"` | `t("audit.crawlingPages")` |
| `"Analysing SEO signals…"` | `t("audit.analysingSignals")` |
| `"Crawl"` | `t("audit.crawl")` |
| `"Audit"` | `t("audit.audit")` |
| `"Audit failed"` | `t("audit.auditFailed")` |
| `"Audit completed successfully"` | `t("audit.auditSuccess")` |
| `"Score Overview"` | `t("audit.scoreOverview")` |
| Score labels | `t("audit.scores.overall")` etc. |
| Stat labels | `t("audit.stats.pagesAudited")` etc. |
| Table headers | `t("audit.tableHeaders.severity")` etc. |
| `"No issues found"` | `t("audit.noIssues")` |
| `"Your site is in great shape — no SEO issues detected."` | `t("audit.noIssuesHint")` |
| `` `Issues (${n})` `` | `t("audit.issuesCount", { n })` |

- [ ] **Step 9: Apply `t()` to `backlinks/page.tsx`**

Namespace: `backlinks.*`

| Hardcoded | Key |
|-----------|-----|
| `"Backlinks"` | `t("backlinks.title")` |
| `"Monitor your link profile, find opportunities, and exchange links."` | `t("backlinks.subtitle")` |
| Tabs | `t("backlinks.tabs.profile")` etc. |
| `"Run your first backlink analysis to get started."` | `t("backlinks.noData")` |
| `"Analyze Backlinks"` | `t("backlinks.analyze")` |
| `"Re-analyze"` | `t("backlinks.reanalyze")` |
| `"Syncing…"` | `t("backlinks.syncing")` |
| `` `Last synced ${date}` `` | `t("backlinks.lastSynced", { date })` |
| `"Never synced"` | `t("backlinks.neverSynced")` |
| Stat labels | `t("backlinks.stats.totalBacklinks")` etc. |
| Table headers | `t("backlinks.tableHeaders.sourceDomain")` etc. |
| `"Hide spam"` | `t("backlinks.hideSpam")` |
| `"No backlinks found yet. Run analysis first."` | `t("backlinks.noBacklinks")` |
| `"← Prev"` | `t("common.previousPage")` |
| `` `Page ${n}` `` | `t("common.page", { n })` |
| `"Next →"` | `t("common.nextPage")` |
| Opportunity statuses | `t("backlinks.statuses.new")` etc. |
| Exchange fields | `t("backlinks.exchange.yourListing")` etc. |
| Request labels | `t("backlinks.requests.sent")` etc. |
| `` `No ${role} requests.` `` | `t("backlinks.requests.noRequests", { role })` |

- [ ] **Step 10: Apply `t()` to `publishing/page.tsx`**

Namespace: `publishing.*`

| Hardcoded | Key |
|-----------|-----|
| `"Publishing"` | `t("publishing.title")` |
| `"Connect your CMS and publish articles directly."` | `t("publishing.subtitle")` |
| `"Add Connection"` | `t("publishing.addConnection")` |
| `"Connections"` | `t("publishing.connections")` |
| `"No publishing connections yet"` | `t("publishing.noConnections")` |
| `"Add a connection to start publishing articles to your CMS."` | `t("publishing.noConnectionsHint")` |
| `"Publish History"` | `t("publishing.publishHistory")` |
| `"No publish jobs yet."` | `t("publishing.noJobs")` |
| `"Edit Connection"` | `t("publishing.editConnection")` |
| Modal titles | `t("publishing.connectionModal.addTitle")` etc. |
| Modal subtitles | `t("publishing.connectionModal.addSubtitle")` etc. |
| Form fields | `t("publishing.connectionModal.name")` etc. |
| `"Save Changes"` | `t("publishing.connectionModal.saveChanges")` |
| Card status labels | `t("publishing.connectionCard.connected")` etc. |
| Job labels | `t("publishing.job.viewPost")` etc. |

- [ ] **Step 11: Verify TypeScript compiles**

```bash
cd apps/web && pnpm tsc --noEmit 2>&1 | head -40
```

Expected: no new errors.

- [ ] **Step 12: Commit**

```bash
git add apps/web/app/\(dashboard\)/\[projectId\]/
git commit -m "feat(web): apply t() to all [projectId] pages (overview through publishing)"
```

---

### Task 9: Stub locale files for fr, es, de, pt, ar

**Files:**
- Create: `apps/web/public/locales/fr/common.json`
- Create: `apps/web/public/locales/es/common.json`
- Create: `apps/web/public/locales/de/common.json`
- Create: `apps/web/public/locales/pt/common.json`
- Create: `apps/web/public/locales/ar/common.json`

**Interfaces:**
- Consumes: `en/common.json` as the key template
- Produces: five locale files with identical key structure, values initially in English (fallback works automatically via i18next `fallbackLng: "en"` until translations are filled in)

The stub approach: copy the English file to each locale. i18next falls back to `en` for any missing key, so stubs with English values are safe to ship — the picker works, language preference saves, and translators can fill in the values incrementally.

- [ ] **Step 1: Create stub locale directories and files**

```bash
mkdir -p apps/web/public/locales/{fr,es,de,pt,ar}
```

Copy the English file to each locale:

```bash
cp apps/web/public/locales/en/common.json apps/web/public/locales/fr/common.json
cp apps/web/public/locales/en/common.json apps/web/public/locales/es/common.json
cp apps/web/public/locales/en/common.json apps/web/public/locales/de/common.json
cp apps/web/public/locales/en/common.json apps/web/public/locales/pt/common.json
cp apps/web/public/locales/en/common.json apps/web/public/locales/ar/common.json
```

- [ ] **Step 2: Verify all locale files are valid JSON**

```bash
for lang in fr es de pt ar; do
  node -e "JSON.parse(require('fs').readFileSync('apps/web/public/locales/$lang/common.json','utf8')); console.log('$lang OK')"
done
```

Expected:
```
fr OK
es OK
de OK
pt OK
ar OK
```

- [ ] **Step 3: Smoke test — start dev server and switch language**

```bash
cd apps/web && pnpm dev &
```

- Open the app, click the `LanguagePicker` in the TopBar
- Select `Français` — the `fennex_lang` cookie should update to `fr`
- Reload the page — i18next should init with `fr` (strings still English from stub, but no fallback errors)
- Open DevTools → Application → Cookies: confirm `fennex_lang=fr` exists with `path=/`
- Switch back to `English` — cookie updates to `en`, PATCH request fires to `/api/v1/users/me/language`

- [ ] **Step 4: Run full frontend type check**

```bash
cd apps/web && pnpm tsc --noEmit
```

Expected: 0 errors.

- [ ] **Step 5: Commit**

```bash
git add apps/web/public/locales/
git commit -m "feat(web): add stub locale files for fr, es, de, pt, ar"
```

---

## Self-Review

**Spec coverage check:**

| Spec requirement | Covered in |
|------------------|-----------|
| 6 languages: en, fr, es, de, pt, ar | Task 3 (en), Task 9 (fr/es/de/pt/ar) |
| Cookie `fennex_lang`, 1 year, `SameSite=Lax` | Task 2 (`lib/i18n.ts` detection config) |
| `I18nProvider` syncs `user.language` | Task 2 (`I18nProvider.tsx`) |
| `GET /users/me` returns `language` | Task 1 (UserProfile + endpoint) |
| `PATCH /users/me/language` validates SUPPORTED_LOCALES | Task 1 (users.py) |
| Alembic migration for `language` column | Task 1 (migration file) |
| `LanguagePicker` in TopBar | Task 4 |
| `LanguagePicker` in Settings Account section | Task 4 |
| `updateMyLanguage` in api.ts | Task 2 |
| `UserProfile.language` in api.ts | Task 2 |
| All pages apply `useTranslation` | Tasks 5–8 |
| Single `common` namespace | Task 3 |
| Fallback to `en` | Task 2 (`fallbackLng: "en"`) |
| 400 for unsupported locale on backend | Task 1 |
| CommandPalette: `t()` in useMemo with `t` in dep array | Task 5 |

**Type consistency check:**
- `Locale` type is exported from `lib/i18n.ts` and consumed by `LanguagePicker.tsx` ✓
- `updateMyLanguage(language: string)` signature matches backend `{ "language": "fr" }` response ✓
- `UserProfile.language: string` matches backend `UserProfile.language: str` ✓
- Migration revision `l7g8b9c0d1e2` correctly revises `k6f7a8b9c0d1` ✓
- `SUPPORTED_LOCALES` on backend matches frontend array `["en","fr","es","de","pt","ar"]` ✓

**Placeholder scan:** No TBD, no "implement later", no references to future tasks — all code is present. ✓
