"""The Fennex roster.

Every module in this package that exposes `EMPLOYEE` (or `EMPLOYEES`) is
discovered and hired automatically by `app.employees.registry`. Adding an
employee to the company means adding a file here -- nothing else in the
application changes.
"""
